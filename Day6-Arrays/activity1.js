// Task 1 : Creation of Array

const arr = [1, 2, 3, 4, 5];
console.log(arr);

// Task 2 : Access first and last element 

const firstElem = arr[0];
const lastElem = arr[arr.length-1];

console.log(`The first element of ${arr} is ${firstElem}`);
console.log();
console.log(`The last element of ${arr} is ${lastElem}`);