// Task 14

const num1 = 6;

const num2 = -8;

num1 > 0 ? (console.log(`${num1} is positive`)) : (console.log(`${num1} is negative`));

num2 > 0 ? (console.log(`${num2} is positive`)) : (console.log(`${num2} is negative`));

