// Task 1 : Printing numbers (1-10) using for-loop

for (let i = 1; i <= 10; i++) {
    process.stdout.write(i + " "); // Used for Node.js: Printing to Console without a Trailing Newline
}

// Task 2 : Print Table of 5 using for-loop

for (let i = 1; i <= 10; i++) {
    console.log();
    console.log(`5 x ${i} = ${5 * i}`);
}